spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/limo_market
    username: conellimo
    password: devpass123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect